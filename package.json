{
  "name": "mafia",
  "private": true,
  "license": "MIT",
  "packageManager": "npm@11.17.0",
  "scripts": {
    "dev": "node --env-file-if-exists=.env apps/web/node_modules/next/dist/bin/next dev apps/web",
    "build": "npm run build --prefix apps/web",
    "bundle": "bash scripts/build-release.sh",
    "check": "npm run check:api && npm run check:web && npm run check:scripts",
    "check:api": "uv run --frozen ruff check apps/api/src apps/api/tests && uv run --frozen pyright && uv run --frozen pytest -q",
    "check:scripts": "bash -n scripts/build-release.sh packaging/bin/install packaging/bin/api packaging/bin/web",
    "check:web": "npm run lint --prefix apps/web -- --quiet && npm run typecheck --prefix apps/web && npm test --prefix apps/web -- --run && npm run build --prefix apps/web",
    "lint": "npm run lint --prefix apps/web",
    "test": "npm run test:api && npm run test:web",
    "test:api": "uv run --frozen pytest -q",
    "test:web": "npm test --prefix apps/web -- --run"
  },
  "devDependencies": {
    "@devcontainers/cli": "^0.88.0"
  }
}
