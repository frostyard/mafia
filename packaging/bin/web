#!/usr/bin/env bash
set -Eeuo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
node_args=()

if [[ -f "$root/.env" ]]; then
  node_args+=("--env-file=$root/.env")
fi

cd "$root/web"
export NODE_ENV="${NODE_ENV:-production}"
exec node "${node_args[@]}" launch.cjs
